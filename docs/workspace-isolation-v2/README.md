# Workspace isolation v2

This repo currently contains only the Harbourview bootstrap README, so this integration lands the workspace hardening pack as the first real schema and server-side control layer.
